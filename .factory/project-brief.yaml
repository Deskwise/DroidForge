version: 1
mode: bootstrap
persona: pragmatic
autonomy: L2
intent:
  goal: Test the DroidForge functionality
  context: Testing the CLI tool to ensure it works properly
  constraints:
    - Keep it simple
    - Focus on core functionality
domain:
  type: typescript-cli
  stack:
    - typescript
    - node
    - cli
preferences:
  testingStyle: unit
  docStyle: markdown
  toolWidening: conservative
signals:
  frameworks:
    - frontend
    - testing
    - backend
  scripts:
    - scripts/test.sh
    - npm:test
  prdPaths:
    - README.md
    - docs/prd/prd.md
    - docs/prd.md
    - docs/existing.md
    - docs/new.md
